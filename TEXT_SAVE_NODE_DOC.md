# Qwen3 文本保存节点文档

## 1. 节点概述

本节点用于将文本数据保存到本地文件，支持txt和excel两种格式。可自动处理文件不存在时的创建、文件存在时的续写，并能将JSON格式的文本数据格式化后存储。

## 2. 功能特性

- 支持txt和excel两种文件格式
- 自动创建不存在的文件
- 支持续写模式（默认）和覆盖模式
- 自动检测并格式化JSON字符串为单行
- 支持指定文件路径和名称
- Excel格式支持指定工作表名称

## 3. 节点类型

| 节点名称          | 节点分类   | 描述                  |
|-------------------|------------|-----------------------|
| Qwen3_Save_Text   | Utilities  | 保存文本到本地文件    |

## 4. 输入参数

| 参数名            | 类型       | 必填 | 默认值   | 描述                                          |
|-------------------|------------|------|----------|-----------------------------------------------|
| text              | STRING     | 是   | -        | 需要保存的文本内容                              |
| file_path         | STRING     | 是   | -        | 保存文件的完整路径（包含文件名和后缀）          |
| file_format       | STRING     | 否   | txt      | 文件格式选择：txt或excel                       |
| mode              | STRING     | 否   | append   | 写入模式：append（续写）或write（覆盖）         |
| sheet_name        | STRING     | 否   | Sheet1   | Excel格式时的工作表名称，仅在file_format为excel时有效 |

## 5. 输出参数

| 参数名            | 类型       | 描述                                          |
|-------------------|------------|-----------------------------------------------|
| status            | STRING     | 保存操作的状态信息（成功或失败原因）            |
| file_path         | STRING     | 实际保存的文件路径                              |

## 6. 实现逻辑

### 6.1 文件路径处理
- 自动创建不存在的目录

### 6.2 文本内容处理
- 检查输入文本是否为JSON格式，如果是则格式化单行输出
- 非JSON格式文本直接保存

### 6.3 TXT文件保存
- 使用open()函数，以a+（append）或w（write）模式打开文件
- 写入文本内容并换行

### 6.4 Excel文件保存
- 使用openpyxl库操作Excel文件
- 如果文件不存在则创建新文件
- 如果文件存在且选择append模式，则在现有工作表末尾追加
- 如果文件存在且选择write模式，则覆盖整个工作表

## 7. 依赖说明

- **txt格式**：无需额外依赖，使用Python内置open()函数
- **excel格式**：需要安装openpyxl库

## 8. 使用示例

### 8.1 保存普通文本到TXT文件
```json
{
  "text": "这是一段测试文本",
  "file_path": "x:/test.txt",
  "file_format": "txt",
  "mode": "append"
}
```

### 8.2 保存JSON文本到TXT文件
```json
{
  "text": "{\"answer\": \"这是一个JSON响应\"}",
  "file_path": "x:/json_data.txt",
  "file_format": "txt",
  "mode": "write"
}
```

### 8.3 保存文本到Excel文件
```json
{
  "text": "这是Excel中的一行文本",
  "file_path": "x:/excel_data.xlsx",
  "file_format": "excel",
  "mode": "append",
  "sheet_name": "Sheet2"
}
```

## 9. 注意事项

1. 文件路径必须包含完整的文件名和后缀
2. Excel格式仅支持.xlsx扩展名
3. 如果选择write模式，会覆盖现有文件/工作表内容
4. 确保对保存路径具有写入权限
5. 处理大文件时建议使用txt格式以提高性能