# Text/Excel 文件读取节点设计文档

## 1. 设计背景
为了支持用户在工作流中循环调用文生图功能，需要实现一套文本/Excel 文件读取节点，支持分批读取文件内容，并能像图像批次读取一样按索引访问文本批次。

## 2. 节点设计

### 2.1 文本/Excel 文件读取节点 (Qwen3_Text_Excel_Reader)
#### 功能描述
读取文本或 Excel 文件内容，支持设置起始行和读取行数，输出文本批次集合和总文本行数。

#### 输入参数
| 参数名 | 类型 | 描述 |
|--------|------|------|
| file_path | STRING | 文件路径 (支持 .txt 和 .xlsx 格式) |
| file_format | STRING | 文件格式选择 (text/excel) |
| start_line | INT | 起始行号 (从 1 开始计数) |
| read_count | INT | 读取行数 (-1 表示读取全部) |
| excel_sheet | STRING | Excel 工作表名称 (默认为 "Sheet1") |

#### 输出参数
| 参数名 | 类型 | 描述 |
|--------|------|------|
| text_batches | BATCH_TEXT | 文本批次集合 |
| text_line_count | INT | 总文本行数 |

#### 实现要点
- 支持 .txt 和 .xlsx 格式
- 自动跳过空行
- 支持读取指定范围的行
- Excel 文件支持指定工作表名称
- 输出为文本批次集合，便于后续批次读取节点使用

### 2.2 文本批次读取节点 (Qwen3_Text_Batch_Loader)
#### 功能描述
读取文本批次集合，支持设置读取索引，输出对应索引的文本内容。

#### 输入参数
| 参数名 | 类型 | 描述 |
|--------|------|------|
| text_batches | BATCH_TEXT | 文本批次集合 |
| batch_index | INT | 读取索引 (0 开始计数) |

#### 输出参数
| 参数名 | 类型 | 描述 |
|--------|------|------|
| text_content | STRING | 对应索引的文本内容 |

#### 实现要点
- 类似图像批次读取节点
- 支持设置索引读取特定文本
- 输出文本内容可直接用于文生图等工作流

## 3. 实现技术方案

### 3.1 文件读取实现
- **文本文件**: 使用 `open()` 函数读取
- **Excel 文件**: 使用 `openpyxl` 库读取

### 3.2 批次处理
- 将读取的文本内容按行组织成批次集合
- 支持后续批次读取节点按索引访问

### 3.3 JSON 处理
- 支持自动格式化 JSON 文本为单行

## 4. 工作流应用
1. 使用文本/Excel 文件读取节点读取文件内容，输出文本批次集合
2. 使用文本批次读取节点按索引循环读取文本内容
3. 将读取的文本内容作为输入传递给文生图节点
4. 实现批量文生图功能

## 5. 代码结构
```
├── text_read_nodes.py       # 实现文本/Excel 读取节点
├── __init__.py              # 注册节点
└── requirements.txt         # 依赖管理
```

## 6. 依赖
- `openpyxl` (用于 Excel 文件读取)