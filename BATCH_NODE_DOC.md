# Qwen3-VL 批量反推节点使用指南 (Qwen3 VQA Batch)

## 1. 节点简介
**Qwen3 VQA Batch** 是专为高吞吐量场景设计的节点。与普通的 `Qwen3 VQA Quick` 不同，它利用了 GPU 的并行计算能力，能够同时处理多张图片，显著减少总推理时间。

*   **适用场景**: 视频抽帧反推、大批量素材打标、连环画分析。
*   **性能提升**: 相比循环调用单图节点，速度提升约 **3-4 倍** (在 RTX 3090 上实测)。

## 2. 参数说明与调优建议 (针对 24G 显存)

| 参数名 | 推荐设置 (RTX 3090) | 说明 |
| :--- | :--- | :--- |
| **images** | - | 连接 `Load Image (Path)` 或 `Load Image Batch` 节点。支持任意数量图片，节点会自动分批处理。 |
| **batch_size** | **8 - 12** | **核心参数**。单次送入显卡的图片数量。<br> - **24G 显存**: 设为 **8** 比较安全，极限可尝试 **12**。<br> - **12G 显存**: 建议设为 **2-4**。<br> *注意: 过大会导致 Out of Memory (OOM)。* |
| **max_pixels** | **768 * 28 * 28** | 图片最大分辨率限制。保持默认值 (768) 可以在速度和细节之间取得最佳平衡。调大此值会显著增加显存占用，需相应调小 `batch_size`。 |
| **max_new_tokens** | **512 - 768** | 每张图片允许生成的最大字数。如果发现描述被截断，可调大此值。 |
| **quantization** | **none** (BF16) | 3090 显存充足，建议使用 `none` 以获得最佳画质。如果需要更大的 batch_size，可改为 `4bit`。 |
| **attention** | **sage_attention_2** | 强烈建议保持默认。它能提供最快的注意力计算速度。 |

## 3. 使用技巧

### 3.1 配合 ComfyUI 列表操作
由于该节点输出的是 **List of Strings** (`LIST`), 你可能需要配合以下节点使用：
*   **Show Text**: 可以直接显示列表内容。
*   **Save Text / Text Save**: 需要支持 List 输入的保存节点。
*   **Primitive**: 将结果转换为字符串列表传递给后续工作流。

### 3.2 显存管理
*   **Keep Model Loaded**: 批量任务开始前，务必勾选 `True`。任务结束后，如果需要运行 SDXL/Flux 等大模型生成图片，建议手动清理显存或取消勾选运行一次空任务以释放资源。
*   **OOM 恢复**: 如果遇到 `CUDA Out of Memory`，请减小 `batch_size` (例如从 8 降到 4)，ComfyUI 通常无需重启即可恢复。

### 3.3 性能对比数据 (实测)
*   **单图循环 (Loop)**: 平均耗时 ~16s / 张
*   **批量并行 (Batch)**: 平均耗时 ~5.5s / 张
*   **加速比**: **~3 倍**

## 4. 常见问题
*   **Q: 为什么输出的文字列表顺序是乱的？**
    *   A: 不会乱。输出列表的顺序严格对应输入图片的顺序。
*   **Q: 输入图片分辨率不一致怎么办？**
    *   A: 节点内部会自动处理。Qwen-VL 支持动态分辨率，不同比例的图片可以混在一个 Batch 里处理（Processor 会自动 Padding）。
